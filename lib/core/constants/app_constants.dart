class AppConstants {
  static const String notesCollection = 'notes';
  static const String tasksCollection = 'tasks';
  static const String storageNotesFolder = 'note_images';
}

enum ViewType { list, card, grid }

enum SortType { latest, alertTime }

enum AppTab { notes, tasks }